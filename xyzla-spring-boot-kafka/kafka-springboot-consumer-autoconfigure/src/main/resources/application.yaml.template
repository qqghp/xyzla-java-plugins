kafka:
  bootstrap-servers: '"KAFKA Bootstrap Servers"'
  client-mark: '"Your Project Name"'
  consumer:
    group-id: '"Kafka Conumser Group ID, 通常取值为 Group-{Proejct Name}"'
mq:
  topic:
    prefix: '"Topic 前缀, 通常取值为 ${spring.profiles.active}"'
